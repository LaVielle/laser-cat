// The setup() function
function setup(){

   // create HTML canvas element
   var canvas = createCanvas(640, 360);

   // Making background black
   background(0);
}

// The draw() function loops at 30fps. This is where interaction and animation happens.
function draw() {
   // Nothing here yet
}
